# Prototype Instructions

2026-09-06 VALIDATED DESIGN RESTORED AND LIVE: user explicitly asked to remedy the visual/navigation divergence found in the prior comparison, not create Figma or another redesign. Published application source `cd4051e0e199fb1004a609000cc1bd2ecbd300ab`, Worker `7bf35112-b45b-45e5-9205-71bd05493752`. Compact five-profile chooser, direct fixed OIDC start via consumed `connexion=1`, shared SchoolShell using original sidebar/logo/brand styles, real student/teacher/parent homes, integrated duty/class/child/media/game-summary/help sections at `/pilote?section=...`, operational admin home/access in same shell, public menu simplified. Role/query navigation never grants server rights. SchoolMedia uses named MediaLibraryView without DemoProvider and namespaced per-account PDF bookmark; original demo wrapper remains. PDF integrated header compact, page visible at y319 on1024/tablet instead of640, rendered page3/navigation/width/resume verified. SchoolShell intentionally uses `school-role-*`, NOT legacy `role-shell-parent` which hides logout ontablet. Explicit work links close create-form mode without clearing drafts; history Back preserves form, and createAssignment uses preserveForm. Build,327 tests and dependency audit pass. Local real fixture tests cover all4 roles1440/1024, draft preservation, class/child/devoir deep links, PDF/audio/video, admin403/logout401; no work/account created. Live actual Auth0 Salma/Lina/parent-Lina sign-ins pass, existing1/2/1 vs1/1/1 works and18/20 review retained; pupil/parent20XP/1grid, live PDF page3 resume, game open/return without new reward, school admin403, logout401. D1 readback all prior counts match:3schools18users2assignments4submissions2reviews1gameaward20XP,10Jasmin active profiles/ZERO active Jasmin sessions. Isolated browsers closed; owner session untouched.21 live pages200/noindex, unknowngame404, APIanonymous401, robotsDisallowall. No schema/secret/account/privilege change or GitHub push. Live authenticated owner admin still NOT retested; local admin only. Direction connected and editorial/manual/othergame modules remain explicitly separate demos, not fake live features. Report `output/playwright/restauration-2026-09-06/COMPTE-RENDU.md`. Codex open of live chooser was queued, not visually confirmed. Preserve preexisting dirty design-qa.md and unrelated untracked files; preserve all earlier legal/security/real-launch reservations.

2026-09-06 NAVIGATION CORRECTION NOW LIVE: user reported missing profile choice and no administrator home. Published source `77b3bace1c13ecb73a1a37c44f20b6749ca7c42c`, Worker `cb889945-702e-4ce4-b214-b902950b4cfb`, no DB migration, secret/account/privilege change or Git push. `/connexion` is now standalone AccessPortal outside DemoProvider: five profiles, Mon compte versus Explorer la démonstration; existing `/connexion/{role}` demo forms preserved. Public Connexion returns here; explicit demo links use `?mode=demo`. Student/parent/teacher choices only change login guidance via `?profil=...`, never server authorization. Direction connected remains unavailable, directs to school information; full direction demo remains available. `/admin` and `/admin/accueil` mount connected AccessAdmin home; `/admin/ecoles-acces` remains management, with persistent nav and validated `?ecole=` deep links. AdminHome derives counts from existing authorized `/api/pilot/admin`, no mock production metrics. PilotApp sends authenticated platform admins to `/admin/accueil`; demo `/admin/pilotage` remains separate. Exact trailing slash and document/history navigation fixed. Build and322 tests pass; local real fixture admin totals match API, school deep link/admin redirect/logout401, browser Back/Forward and5 demo forms pass. Profile chooser and admin home captured1440x900/1024x768 without horizontal overflow. Live chooser5 profiles,7 direct URLs200, anonymous admin401, real Salma Auth0 login via chooser, existing2 submissions/1 review, admin403 and no admin metrics, logout401 verified; isolated browser closed. Live authenticated owner home still requires owner login, NOT verified. Owner Auth0 tab untouched; new chooser tab21 opened in Codex. Report `output/navigation-2026-09-06/COMPTE-RENDU.md`. Prior deployment checkpoint below is historical; preserve all its launch/security/legal reservations.

2026-09-06 CURRENT LIVE CHECKPOINT: published and verified on the existing test Worker, final version `a2ded34f-8f0d-439f-bf72-20bd65027c5f`, source commit `b6767fb1b7c302601675d5fe29c0dd485505331e`. Owner renewed Cloudflare named profile `jetdencre-test`; use pinned Wrangler 4.129.0, no more OAuth request needed unless access actually fails. Real D1 exports before/after additive migration 0004 were saved outside OneDrive/Git with user+SYSTEM-only ACL; both restored successfully into temporary LOCAL SQLite, NOT a remote D1 restore drill. All 21 durable preexisting tables unchanged between exports (sessions/auth state excluded). Export verifier now permits only exact D1 metadata `DELETE FROM sqlite_sequence`; application deletes remain forbidden. Initial live smoke test found guide/direct game 404; fixed Worker routing to shared 14-page public manifest plus exact pilot/game paths, unknown routes remain 404. Rebuilt exact final commit in independent Windows checkout: frozen offline install, build, 321 tests, schema export pass; PDF identical/private files absent/clean tree. Real Auth0/D1: Salma retains 1 assignment/2 submissions/1 review18; Lina completed facile01 once,20XP, reload+second independent browser resume; repeated completion+0; parent Lina sees only her child20XP/1grid; Adam and Nora0XP/0grids, Nora other-class assignment404; teacher/parent admin and pupil-progress access403. Both isolated browsers logged out401 and closed. Remote counts confirm 1 game progress/1 award/20XP, no change to core counts, all10 Jasmin profiles active and ZERO active Jasmin sessions. Owner admin page reopened but requires sign-in; NO authenticated admin recette this turn, no owner logout/password handling. 14 public pages and pilot documents200/noindex, robots Disallow-all. Final app console0 errors; Auth0 favicon404 observed earlier, cosmetic. Report `output/publication-2026-09-06/COMPTE-RENDU.md`. No Git push this turn. Remaining launch gates: owner admin readback, isolated remote restore/rollback, bounded class-load and remaining security/a11y/pedagogical checks, real legal/support/domain and school-pilot validation. Do not duplicate accounts, assignment, submissions, reviews or Lina reward. Prior entries below are HISTORICAL and superseded where they say not deployed/auth blocked.

2026-09-06 FINAL LOCAL CHECKPOINT: application code committed as `cb5c787a5c73b577efaabc7a15cc7224cd028e10`; rebuilt in an independent Windows checkout, frozen offline install, build, all 321 tests and Drizzle export pass, clean Git tree, public PDF byte-identical and private files absent. Follow CI order BUILD BEFORE TESTS (artifact tests require dist). Main workspace final build and Wrangler dry-run pass; still NO deployment, D1 migration or Git push. Clean-checkout evidence: `output/ameliorations-2026-09-05/RECONSTRUCTION-GIT-2026-09-06.json`. Late changes after this source checkpoint are delivery documentation only. Owner session preserved; isolated `jasmin-final` demo browser closed. Vite was left serving the existing local project. Next required external action is Cloudflare reauthorization, then private backup/preflight, additive migration 0004, existing test Worker deployment and synthetic Auth0 recipe. Do not claim launch readiness or repeat the prior school/account/assignment creation.

2026-09-06 ACTION PLAN IMPLEMENTED LOCALLY, NOT DEPLOYED. See `LIVRAISON-PILOTE-2026-09-06.md`. Auth-start quota fix verified (atomic conditional peer/global batch, monotonic window, no peer allocation at full global budget); 321/321 tests pass on Node 24.20.0. New exact `/pilote/jeux/mots-fleches` server-backed route, migration `0004_pilot_mots_fleches.sql`, own-school/student revisioned progress, one server-verified award per grid, 20/35/50 XP (630 total), parent-only authorized-child summary. Migration from schema 0003 preserves every preexisting table in a local SQLite test. Game outbox is per-tab `sessionStorage` key `jde-school.crosswords.v1:` outside demo reset: reload survives, closing an unsynced tab may lose pending work; do not promise permanent offline storage. Local real-handler game completion (+20), reload, offline recovery/two-tab conflict, parent summary and other-school isolation verified in isolated Edge sessions, then school logout confirmed. PDF page 5/150% resume verified in demo media only. Public Connexion now goes to `/pilote`, demo remains separate, manual Jet d’Encre admin provisioning unchanged; `/guide-ecole` added; 14 public pages prerender, staging remains noindex and legal identity still fictitious. Re-read 222 entries, integrated initial 30-entry editorial patch plus 57 compact cell clues; answers/geometry/IDs/XP unchanged. All 18 grids × 3 viewport sizes (1024x768,1366x768,1440x900) checked without actual clue/page overflow; eight contrast views pass automatic checks, not WCAG certification. Reports under `output/ameliorations-2026-09-05/`, captures `output/playwright/launch/`. Historical sealed audit remains untouched; complementary security scan `a8ecc449-af04-41e2-925a-4775ba6ae5a9` covers 24 full files/four surfaces only, tied to initial snapshot, not exhaustive/final-commit assurance. Backup verifier restores ONLY temporary local SQLite, no remote D1 export/restore performed. Cloudflare named profile `jetdencre-test` is listed but deployments-list has no usable noninteractive authentication; simulation succeeds, actual publication/migration require owner reauthorization. Do not create a temporary account, another Site or paid service to bypass this. Live Worker remains previously verified version `3c8e0936-cf92-467c-a310-7cb159cbe6d5`; owner session untouched. Prior audit-only entry below is historical. Preserve preexisting unrelated `design-qa.md` and untracked design/report/media files.

2026-09-05 AUDIT ONLY completed; no product fix, deployment or Git push. Full suite 269/269, build and package audit pass. Durable account/report and prioritized next steps: `output/audit-2026-09-05/COMPTE-RENDU-AUDIT-2026-09-05.md` and `PLAN-ACTION-2026-09-05.md`. Security scan ada82bd0-2b9c-49f9-8470-70d8d38a5b30 sealed, partial coverage (44 fully security-reviewed files; remaining paths recorded), one MEDIUM availability finding: worker/pilot/oidc.js:30 increments the shared start budget before the per-peer guard; isolated in-memory reproduction admits 12, rejects 288, exhausts global=300 and rejects a fresh peer. Never reproduce saturation on the live Worker. MotsFlechesGame fast keyboard entry can overwrite letters due to requestAnimationFrame focus lag; normal 180ms typing succeeds, word revealed only after correct solution. Tablet1024x768 grid heading overlaps tabs. PDF navigation/zoom/media playback pass but PDF reload resets page/zoom. Four of six axe views have contrast issues; pilot teacher/parent views have no automatic violations. Homepage Connexion still enters demo; actual games/XP remain separate from server pilot. Real synthetic Salma, parent Lina, Nora sign-ins and read-only scope checks pass; final logout returns401 and jasmin-audit browser closed. Owner session preserved; no duplicate assignment, submission, review or account created. Durable canonical security artifacts copied unchanged under output/audit-2026-09-05/securite. No claim of load test, restore drill, legal approval, physical tablet testing or exhaustive source security audit.

2026-09-05 French-login and count-label finishing pass is DEPLOYED to existing Cloudflare test Worker, version `3c8e0936-cf92-467c-a310-7cb159cbe6d5`. Auth0 tenant friendly name is Jet d’Encre, default French fr-FR; worker explicitly requests ui_locales=fr-FR while keeping prompt=login, PKCE, nonce and all prior guards. Universal Login has French custom login title/description/labels, existing horizontal logo (height 76), navy button/links, ivory background, gold focus and existing JE favicon URL. No credentials, connections, signup permissions or paid options were changed. Pilot summary and admin sidebar/heading now use singular at count 1. Build and all 269 tests pass. Real published sign-ins for parent Lina (1/1/1), Salma (1/2/1), Nora (0/0/0) succeeded. Admin 0/1/2 labels separately passed browser-only API fixtures, not actual admin creation. Final French-login screenshots at 1440x900 and 1024x768 are visually checked, no horizontal/vertical overflow, final login-page console 0 errors/warnings. Ten school profiles remain active/linked with zero sessions; owner admin session preserved and reloaded. CLI test session jasmin-finitions is closed. Cloudflare deployment-list and deploy worked without reauthorization; the previous 7403 issue was not encountered here, but no fresh D1 admin SELECT is claimed. Details: `audit-lancement-2026-09-04/FINITIONS-CONNEXION-2026-09-05.md`. Existing private Jasmin helper now uses observed French login labels. Do not rerun assignment-creation scripts.

The owner explicitly authorized Playwright and then requested downloading/installing its missing prerequisites. On 2026-09-05 installed verified official Node 24.20.0 (npm/npx 11.19.0) at `%LOCALAPPDATA%/Jetdencre/Tools/node-v24.20.0-win-x64` and @playwright/cli 0.1.19 at `%LOCALAPPDATA%/Jetdencre/Tools/playwright-cli`, without changing permanent PATH or app dependencies. Existing Edge runs isolated Playwright CLI contexts. The CLI-first helper `.local-data/jasmin-cli.mjs` uses explicit executable paths, reads only authorized synthetic credentials, redacts output, and removes private temporary login snippets. Never print the passwords or inspect/export owner cookies. User approval to use this tool for existing synthetic-account logins is already given; do not request it again.

JASMIN LIVE RECIPE COMPLETED: all ten imported accounts successfully signed in via real Auth0. One Salma assignment `9490876a-9741-4350-8495-49da87b1065c` (Jasmin · Mon lieu préféré à l’école, due 2026-09-12) has two submissions (Lina and Adam), one 18/20 review for Lina (submission `a156223c-3660-4b37-ace7-7ef3f7089af2`). Teacher/student/parent persistence, draft preservation on in-page Actualiser, same-class personal/family isolation, five second-class accounts’ empty workspace and direct 404 access, and school-account admin 403 were verified. Adam's active session was suspended (1→0 sessions, live workspace 401, sign-in screen), then account reactivated without reviving the old session (still 401), then re-login restored only his own work. All ten profiles remain active/linked with ZERO school sessions confirmed in the live admin UI. Both Playwright sessions `jasmin-teacher`/`jasmin-learner` were logged out (401 verified) and closed. Owner's admin session remains open. No app deployment or D1 admin read; previous Cloudflare CLI 7403 is still a future administration prerequisite. Private generated text files were checked: no plaintext test passwords, no temporary login snippets left. Detailed evidence and minor UX follow-ups: `audit-lancement-2026-09-04/RECETTE-JASMIN-2026-09-05.md`. Do not rerun the creation/remise scripts or duplicate the existing test assignment.

Jasmin live-journey continuation: the available CUA browser is only iab id 1, with visibility/viewport capabilities and no independent-session capability. Attempting a separate Chrome test tab returned `Browser is not available: chrome`; no login/logout or change to the owner session occurred. Do not claim a Jasmin live learning journey. The current CUA workflow requires an explicit user request before changing UI-control technology. Request permission to use Playwright for an independent test browser and the existing synthetic credentials (not new passwords or owner credentials), then follow its skill if authorized. Added one meaningful test to existing `tests/pilot.test.mjs`: same-school two-class teacher/student/parent isolation, direct assignment URLs, unauthorized submission/correction/publication, and valid second-class access in both directions. Full suite is now 269 passes. No deployment performed; new coverage is local automated evidence, not a live OIDC result.

The Jasmin Auth0 import is now COMPLETE and its ten identities are linked (2026-09-05). The owner submitted job `job_FqCJMkklRv7OmpQ9`; Auth0 More info showed completed, inserted 10, updated 0, failed 0, total 10. Read each actual name/email/subject from Users, then linked via the owner-authenticated Jet d’Encre UI. Reload confirmed ten Connexion rattachée statuses and ten matching history actions. Exact mapping: `audit-lancement-2026-09-04/IMPORT-JASMIN-2026-09-05.md`. The private password note was updated without changing or printing passwords; import files must not be resubmitted. All ten Jasmin profiles still show zero sessions: no first Jasmin login or full learning journey is verified. Owner remains signed in; do not discard that session for a school-login test without a distinct browser session or an agreed handoff. Cloudflare CLI remote D1 returned 7403 again, while the live site UI continues to work; reauthorization of the existing named profile will be needed before direct D1 administration/deployment. No fresh D1 readback is claimed. This supersedes the older import-pending entries below.

The owner could not locate the hidden AppData import file. A byte-identical, verified copy of ONLY `auth0-import-jasmin.json` now exists at `C:/Users/mouat/Downloads/Jetdencre-test/auth0-import-jasmin.json` (10 accounts, 3643 bytes), outside OneDrive and Git, in a folder restricted to the owner and SYSTEM. Direct the owner to this accessible copy for the Auth0 handoff; the plaintext password note remains only in its original private AppData folder. This is a filesystem copy, not an Auth0 upload or import. Do not regenerate credentials or overwrite a differing import file.

On 2026-09-05 the owner explicitly authorized autonomous routine development, reversible tests, and selection of fictional test emails and passwords with a private local note. Do not ask for repeated approval for in-scope free synthetic tests. Continue to require the applicable browser credential handoff and action-time security approvals; this does not authorize paid services, real pupils' data, arbitrary account elevation or destructive production work. Ten distinct 32-character synthetic Jasmin credentials and a PBKDF2-SHA256 Auth0 import JSON are PREPARED ONLY at `%LOCALAPPDATA%/Jetdencre/PrivateTestAccounts/Jasmin-2026-09-05/`, outside OneDrive and Git, with folder access limited to the current Windows user and SYSTEM. The note contains plaintext passwords at the owner's request: never print, index, commit, upload or open it in a browser. Do not regenerate or overwrite these credentials. The import has NOT been uploaded or submitted; user handoff is needed on the existing Auth0 Import Users page, with Username-Password-Authentication selected and upsert/completion-email unchecked. After user import, verify actual Auth0 subjects before binding school profiles. No new Jasmin identity is mapped yet. Latest verification: all 268 automated tests pass, build passes, dependency audit reports no known vulnerabilities, five live readiness/anonymous-access probes pass. The live admin UI suspended and reactivated the synthetic Adam profile; reload and remote D1 confirm 10 active Jasmin profiles, zero Jasmin identities, one event of each action, valid foreign keys and one active owner session. The browser may time out after a native confirmation despite successful server mutation: inspect dialog with Browser.tabs.get(id).getJsDialog(), then reload/read back rather than repeat the write. Details: `audit-lancement-2026-09-04/AUTONOMIE-ET-TESTS-2026-09-05.md`.

On 2026-09-05 the owner authorized preparation of a complete synthetic school. Created through the live owner-authenticated administration UI, not by direct SQL: `École Jasmin · pilote fictif` (id `55e97707-ff0a-4a82-b091-595ffb905ccf`), two classes `4e AEP · Jasmin` and `5e AEP · Jasmin`, teachers Salma and Yassine respectively, pupils Lina/Adam in 4e and Nora/Ilyas in 5e, and one separate fictional parent linked exclusively to each pupil. Ten school profiles total; no Auth0 identities or passwords created for them, all display `Connexion à préparer`. Reload and independent remote D1 reads confirm six class memberships, four active family links, no identity links for this school and valid foreign keys. The UI history shows all thirteen creation actions. Existing schools A/B and the owner session were preserved. Do not duplicate this school on continuation. Details: `audit-lancement-2026-09-04/ECOLE-JASMIN-PILOTE.md`. Browser CUA documentation is available after js_reset + one getTab initialization; use supported Tab.playwright locators for repetitive form operations, and markDeliverable/markHandoff to preserve agent-created tabs across turns.

The owner's first real administrator login is now verified (2026-09-05). The live `/admin/ecoles-acces` UI displays both synthetic schools (A: 4 accounts, B: 3), the four correctly linked school A accounts, and the school/class/account creation, suspension and history controls. Independent D1 readback confirms exactly one active OIDC session for `pilot-platform-owner`, an active administrator grant, no school membership, two schools, seven school accounts, six identities and valid foreign keys. Keep the owner's active session open for their use. This verifies authenticated loading and visibility of administrator controls, not a completed browser test of every write action; no school, account, suspension or password was changed during this verification.

Owner bootstrap completed on 2026-09-05 after manual Auth0 account creation. The observed account mouataz.pro@gmail.com has exact subject `auth0|6a9c6b2b4d1cd016ace638f7` in Username-Password-Authentication. Its distinct D1 profile `pilot-platform-owner` is active, mapped under `https://dev-i16kabawdbdi2mk0.us.auth0.com/`, and has the only active `pilot_admins` grant, with no school membership. Four guarded bootstrap statements each inserted one row (user, identity, grant, journal), and independent remote readback confirmed 1 admin, 6 identities, 8 users, no foreign-key anomaly and zero owner sessions. No other profile was promoted. The bootstrap SQL lives only in ignored `.local-data/bootstrap-owner.sql` and must not become a migration or production asset. This supersedes the pending-owner-bootstrap state below. Owner's first real application sign-in is still pending: the administrator page is being opened for password-only handoff. Do not claim the admin UI journey verified until after the owner signs in. Never inspect their password, cookies or tokens.

The 2026-09-05 access-management decision is durable: Jet d’Encre platform administrators create schools, assign each school account and hand credentials over manually. No public enrolment or user-selected school attachment. `/admin/ecoles-acces` now runs outside DemoProvider on the pilot server, with an explicit `pilot_admins` grant independent of school roles. Migration `0003_platform_admin.sql` adds grants and an action journal. The only seeded administrator is a loopback-only fixture; never deploy or seed it remotely. School/account suspension revokes the affected sessions. School profiles, classes and parent-child links can be created here; Auth0 account creation and password operations remain manual, and the UI must state that pending profiles cannot yet sign in. Identity linking accepts only a manually verified Auth0 subject against the server-configured issuer and must not replace existing mappings. The owner's chosen administrator address is mouataz.pro@gmail.com. On the last observed Auth0 Users page only the five synthetic accounts existed: owner account creation and exact-subject verification are pending. No remote administrator grant exists yet. Bootstrap a separate owner profile and explicit grant only after observing that account's exact Auth0 subject; never promote a test teacher or infer a subject from an email. Version `beb49ad5-b7e0-47c1-8605-44085a1fbbae` is deployed to the existing free Cloudflare test Worker; the administrative page returns 200 for HTML navigation, readiness 200, anonymous admin API 401, local fixture API 404. Five previous identity mappings and two schools are unchanged, foreign keys valid. Full suite: 268 passes; final build and targeted pilot/OIDC/Worker tests pass. No authenticated production administrator journey has been tested yet.

Run the local server yourself and open the preview in the browser available to this environment. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

Preserve every existing Pen screen and prototype version. New design waves must be added as clearly named copies or new top-level frames; never overwrite the earlier visual references.

Prioritize complete desktop 1440×900 and tablet 1024×768 experiences. Mobile is a later phase and must not replace or reduce the desktop/tablet scope.

Keep the established Jet d’Encre balance: navy, gold and ivory carry the premium editorial brand; teal, saffron and terracotta remain functional or pedagogical accents. Continue using Inter for operational UI and Outfit for friendly display headings.

The 2026-08-26 audit decision is durable: preserve the current landing page and visual charter, prioritize complete end-to-end behavior over adding more decorative tabs, keep demo-only behavior explicitly honest, and treat desktop plus tablet accessibility as release criteria. Add a Parent space unless the parental-progress promise is removed from public messaging.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

The 2026-08-26 quiz decision is durable: the selected Culture générale direction is option 1, an editorial full-screen experience with an ivory base, a minimal top bar, a centered question, a segmented circular 10-second timer, a 2×2 answer grid on desktop and stacked answers on mobile. Keep speed secondary to comprehension, pause the timer after selection, show a short pedagogical explanation, and award 10 XP for each correct answer.

The 2026-08-26 class-challenge decision is durable: class competitions stay inside one class and display only randomly assigned, non-identifying pseudonyms. Never expose student names, initials, photos, contact details, or messaging in challenge leaderboards. Rank by correct answers, keep equal scores as explicit ex aequo results, show unfinished participation without assigning a rank, and use only published question banks. Award quiz XP through the shared idempotent XP mechanism so replay or refresh cannot grant the same reward twice.

The 2026-08-27 game-catalog decision is durable: Projet DÉBAT belongs in “Mes jeux” for both students and teachers. Keep one isolated shared game runtime, preserve the distinct return path and local session for each role, and keep the teacher catalogue focused on preparation, projection, differentiation, and formative assessment.

The 2026-09-01 arrowword decision overrides and retires every Agence Plume and old crossword direction. The game is a true magazine-style « Mots fléchés » experience: clue boxes and arrows live inside the grid, answers run only left-to-right or top-to-bottom, and there is no detective story, mascot, investigation scene, external clue list, timer or speed bonus. Keep the grid dominant and readable, highlight only the active answer, accept unaccented keyboard input while displaying correct French accents, and award XP once only after the entire grid is correct through the `mots-fleches` reward namespace. Preserve the three validated levels and rewards: Facile A1, 11 words, +20 XP; Normal A1–A2, 13 words centered on Morocco, +35 XP; Difficile A2–B1, 13 words on environment and citizenship, +50 XP. Each grid cell uses a concise magazine clue while the complete researched definition appears in the selected-clue band; replay never grants the same XP twice. Content-layout versions may change without resetting the once-only XP reward version.

Build app UI in `src/`. Keep `.openai/hosting.json`, `worker/index.js`, `scripts/prepare-sites-build.mjs`, and `tests/sites-worker.test.mjs` intact so the same local prototype can be handed to Sites. Before a Sites handoff, run `npm run build` and `npm run test:sites`; the build must leave `dist/client/index.html`, `dist/server/index.js`, and `dist/.openai/hosting.json`.

The 2026-09-04 launch decision permits fictitious operator and pilot-school details during preparation only. Label them explicitly as demonstration data and never treat them as validated legal information or working support contacts. Do not enable real student-data collection or public indexing on that basis.

The user-supplied Momo PDF is a local reader test, not a published school resource. Keep it and its page captures out of Git and production assets; use `.local-media/` and the loopback-only preview route. Confirm editorial suitability, accessibility and publication rights separately before any public distribution.

The 2026-09-04 shared-learning pilot lives at `/pilote`, outside DemoProvider. Preserve server-derived identity, school/class/family scoping, revocable HttpOnly sessions and idempotent writes. Local fixture login and `.local-data/` must never ship in the Worker. Keep `PILOT_ENABLED=false` until the real OIDC provider, provisioning, hosting and data responsibilities are approved and tested. The owner has free Cloudflare and Auth0 accounts; the dedicated Auth0 test application exists, but no end-to-end connection is verified and no custom domain is confirmed. Remain on free plans, use synthetic school data only, and do not activate billable services or R2 checkout without explicit approval. Preserve the existing Sites project independently of preparation for the owner's Cloudflare account. Written assignments do not award automatic XP; existing demo XP must not be presented as server-verified progress.

The owner subsequently approved the clearly described next stage with **« ok vas y »**: publish a free Cloudflare test version with a separate D1 database, Auth0 and synthetic school profiles. This supersedes the earlier preparation-only decision. It does not authorize paid services, real school data, custom DNS changes or replacement of the existing Sites project. Keep the pilot disabled until secrets, supervised identity mappings and access controls are ready for the controlled test. `wrangler.test.example.jsonc` remains an incomplete example; use a dedicated local configuration for the new test resources. Keep access credentials out of Git and chat; require OS keyring storage and action-time confirmation before a new persistent authorization. Auth0 reauthentication is requested with a server-fixed `prompt:login`, but real-provider behavior and shared-device sign-out still require browser testing. See `audit-lancement-2026-09-04/PREPARATION-CLOUDFLARE-AUTH0.md` for historical preparation state and `audit-lancement-2026-09-04/ESSAI-CLOUDFLARE-AUTH0.md` for the current stage.

The free Cloudflare demonstration is published at `https://jetdencre-test.mouataz-pro.workers.dev`, with dedicated D1 `jetdencre-test-db` and seven synthetic school profiles. Do not create duplicate resources. Use the named `jetdencre-test` Wrangler profile and ignored `wrangler.test.local.jsonc`; never implicitly provision another account/database or replace the existing Sites resources. Auth0 callback and Authorization Code-only changes were saved by the owner and checked. Following the owner's authorization to apply the described settings, the pilot is now enabled for the supervised synthetic test in version `052d6241-cc08-4666-93c7-d6e394e8f33b`, with the exact PILOT_ORIGIN. The example configuration stays disabled. Cloudflare confirms the OIDC_CLIENT_SECRET binding. The Auth0 Credentials accessibility tree unexpectedly exposed its masked value in a tool response: do not repeat, copy, search for or persist that value, and never request an accessibility tree or initial getTab snapshot on Credentials again. No Auth0 CLI administration access is needed for this small setup. The standalone test seed SQL must never run as a migration, be imported by the Worker or be applied to production.

The owner declined rotation with **« pas la peine de la renouveler, juste continue si c'est possible »**; retain it as a recommendation without repeating the question. This supersedes the earlier rotation prerequisite, not the prohibition on reading, copying or persisting the exposed value. The three owner-created Auth0 accounts are verified and mapped with the documented issuer: `enseignant.a@example.com` / `auth0|6a9b580417e4a442c8c23971` → `pilot-a-teacher`; `eleve.a@example.com` / `auth0|6a9b58caf80dd06cfc010835` → `pilot-a-student`; `parent.a@example.com` / `auth0|6a9b590b51d35a791c270d8c` → `pilot-a-parent`. Each guarded INSERT added exactly one row; remote read-back confirms correct roles/school A and valid foreign keys. The parent has only the intended active family link to `pilot-a-student`. With the owner's latest explicit settings authorization, Disable Sign Ups is enabled and saved on the shared database connection (also affects Default App); Google is disabled only for Jet d’Encre — test, with the database connection preserved. Both persisted settings were checked. After pilot activation, 12 anonymous HTTP checks passed: OIDC session endpoint is 200 anonymous, workspace/writes are 401, local login is absent, cross-site auth and auth POST are 403, no CORS access or cookie set in these probes. Real login is NOT verified: the in-app browser leaves the login link unchanged and direct navigation to the observed /api/pilot/auth/start fails with net::ERR_BLOCKED_BY_CLIENT. Do not bypass that browser restriction; the next handoff is opening /pilote in the owner's usual browser and signing in as the synthetic teacher. Latest aggregate check: three identities, zero active sessions, zero active auth flows. Keep the pilot restricted to synthetic tests; revert PILOT_ENABLED=false if anonymous access leaks data, configuration becomes inconsistent or a real login opens the wrong identity. Do not artificially verify example.com addresses or add an MFA requirement not established by the provider. Never inspect password entry during handoff.

The 2026-09-05 first real auth-start attempt exposed a Cloudflare-runtime incompatibility: the guarded OIDC fetch used `redirect: "error"`, which workerd rejects before contacting Auth0. Preserve the compatible and equally strict implementation in `worker/pilot/oidc.js`: use `redirect: "manual"`, then cancel and reject any already-redirected, opaque-redirect or HTTP 3xx response before reading it. The origin allowlist and discovered-endpoint validation remain mandatory for discovery, token and JWKS requests. Deployment `e38612b6-cbe3-491b-ac6f-b1d47c7546b0` contains the fix. Build, dependency audit and 267 tests pass, including discovery and token redirection rejection; the exact bundle also returns 303 under remote workerd. Production checks now return live/ready 200, anonymous session 200, and auth start 303 to the expected Auth0 `/authorize` path with an HttpOnly, Secure, SameSite=Lax transaction cookie. The Auth0 login form was opened visibly for the owner. This verifies the start of the OIDC flow, not a completed sign-in or role mapping: no active session existed at the last check. Do not inspect or fill the owner's password; after the owner submits the synthetic teacher credentials, verify only the resulting Jet d’Encre role and server session.

The supervised synthetic OIDC journey is now complete. The owner entered every password privately; no password, cookie, authorization header or secret was inspected. Teacher A was recognized as `pilot-a-teacher` in school A, published exactly one assignment titled “Mon quartier en quatre phrases”, and logged out; the server session was confirmed revoked. Student A was then recognized as `pilot-a-student`, saw the single class assignment with no teacher controls, submitted one four-sentence fictional response, and the submission persisted after refresh. Teacher A signed in again, saw that single submission, published one immutable 17/20 formative review, and the review persisted after refresh. Parent A was recognized as the parent of student A, saw the same assignment, submission and review, had no create/submit/review controls, and the 1 assignment / 1 submission / 1 review view persisted after refresh. The parent view remains open for the owner. A remote D1 read after the student step confirmed exactly one assignment and one submission linked to school A/class A/teacher A/student A; the post-review administrative SELECT could not run because the Wrangler Cloudflare profile returned authorization error 7403. Do not treat this CLI authorization failure as an application outage: the live server-backed UI completed and refreshed correctly. Reauthorize the named Cloudflare profile before the next D1 or deployment administration step, without opening or copying any secret.

The Cloudflare CLI authorization issue is resolved. The initial consent asking for 29 permissions was cancelled; the named `jetdencre-test` profile was reauthenticated with only `user:read`, `account:read`, `workers_scripts:write`, `d1:write` and the provider-added `offline_access`, stored through the Windows keyring. Two additional owner-created Auth0 test accounts were conditionally mapped exactly once: `eleve2.a@example.com` / `auth0|6a9c52bc905bdd3fa080aba3` → `pilot-a-other`, and `eleve.b@example.com` / `auth0|6a9c531156fde8188d339ae3` → `pilot-b-student`. The supervised isolation test passed: student 2 of school A saw only their own submission and no first-student review; parent A still saw only student 1 and the 17/20 review; student B saw an empty 0/0/0 workspace with no school A data, including after refresh. Final D1 state is five distinct mappings for five profiles, one school A assignment, two school A submissions, one review limited to student 1, no school B assignment, zero active sessions, zero active auth flows and no foreign-key anomaly. Every test session was logged out. A manual direct-link request to school A while signed in as school B was blocked by the in-app browser itself and is not claimed as a live proof; the existing automated isolation test covers that request with a 404 response. Do not provision a multi-school identity before implementing an explicit school selector: the current runtime selects the first active membership ordered by school id.
