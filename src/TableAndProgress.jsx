// Original shared Jet d’Encre presentation, extracted without changing markup.
export function ProgressBar({value,label}){const safe=Math.max(0,Math.min(100,Number(value)||0));return <div className="progress-wrap"><div><span>{label}</span><strong>{safe}%</strong></div><div className="progress-track" role="progressbar" aria-label={label} aria-valuemin="0" aria-valuemax="100" aria-valuenow={safe}><i style={{width:`${safe}%`}}/></div></div>}
export function DataTable({headers,rows,label="Tableau de données"}){return <div className="data-table"><table><caption className="sr-only">{label}</caption><thead><tr>{headers.map((header,index)=><th scope="col" key={`${String(header)}-${index}`}>{header}</th>)}</tr></thead><tbody>{rows.map((row,index)=><tr key={index}>{row.map((cell,cellIndex)=><td key={cellIndex}>{cell}</td>)}</tr>)}</tbody></table></div>}
